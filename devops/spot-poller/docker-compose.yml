services:
  seed-poller:
    build: .
    container_name: stacktrakr-seed-poller
    restart: unless-stopped
    volumes:
      - ../../data:/data
    env_file:
      - .env
    environment:
      - DATA_DIR=/data
      - TZ=America/New_York
